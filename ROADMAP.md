# day3box 개발 로드맵

할 일을 쏟아내고(brain dump), TOP 3를 골라, 시간 블록으로 배치해 하루를 계획하고 실제 달성을 기록하는 타임박싱 플래너.

## 개요

- **제품 정체성**: **좌(리스트) → 우(시간 그리드)** 흐름. 할 일은 먼저 목록으로 태어나고(brain dump → TOP 3 → 소요시간), 시간 위에 배치되며 블록이 된다. 좌측 절차를 빼면 그냥 캘린더다 — 좌측이 곧 타임박싱이다 (PRD 1.3).
- **v0 목표**: 개발자 본인 1인의 개인 도구. 불특정 사용자 확보는 목표가 아니다.
- **v0 성공 기준**: 배포된 상태에서 **본인 연속 3일 실사용** (아침 계획 brain dump→TOP3→배치 + 저녁 상태 기록). 이 기준 통과 전에는 어떤 확장 기능(사진·차트·공유)도 시작하지 않는다 (PRD 1.6).
- **개발 순서 원칙**: "골격 전체 → 더미 UI → 실기능" 구조 우선 접근을 쓰지 않는다. **각 Task는 UI와 DB 영속성이 함께 완성되는 수직 슬라이스**이며, 눈에 보이는 산출물로 끝난다. (근거: brain dump가 새로고침에 날아가면 실사용 이탈 → v0 성공 기준 자체가 위협받는다. PRD 4.1 길 B.)

## 정본 문서

이 로드맵은 아래 정본 문서의 결정을 그대로 반영한다. 충돌 시 우선순위: **PRD_2 = ROADMAP_v1 > 디자인 핸드오프 > Lean Canvas**.

| 문서                                                                                 | 역할                                                                     |
| ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------ |
| [docs/PRD_2.md](docs/PRD_2.md)                                                       | v0 범위(F1~F9)·데이터 모델·기술 스택·UX 파라미터 확정본. **최우선 정본** |
| [docs/ROADMAP_v1.md](docs/ROADMAP_v1.md)                                             | v0→v1→v2 단계 구조, Task 1~9 순서, 단계 이동 규칙                        |
| [docs/LEAN-CANVAS.md](docs/LEAN-CANVAS.md)                                           | UVP("시간 축"과 "사진")·리스크 R1~R4·기능 추가 판단 필터                 |
| [docs/handoff-v0-003/README.md](docs/handoff-v0-003/README.md) | Broadsheet 디자인 토큰, 화면 명세, v0 스코프 경계, 엣지 케이스           |

> **코드 작성 공통 규칙**: 이 프로젝트의 Next.js는 **16.2.11(breaking changes 있음)**이다. 코드를 작성하는 모든 Task는 착수 전에 `node_modules/next/dist/docs/`의 해당 가이드(App Router는 `01-app`)를 먼저 읽고, 학습 데이터의 구버전 관행 대신 설치된 버전의 규약을 따른다 (AGENTS.md 규칙).

## 단계 구조 (가설 검증 로드맵)

```
v0 — 개인 도구 / 단독 가치 검증        ✅ 통과 (2026-08-02, 연속 3일 실사용)
      ↓  통과 조건: 본인 연속 3일 실사용
v1 — 사진 + 공유 산출물 / 실질적 런칭   ← 현재 위치 (R3 검증 단계 — Phase 9)
      ↓  통과 조건: 본인 외 사용자의 이미지 다운로드·공유
v2 — 프라이빗 서클 / 성장·리텐션 엔진
      ↓  통과 조건: 그룹당 주간 활성 멤버 수 (북극성 지표)
v3+ — 차트·반복 블록·알림 등 (논의 자체를 보류)
```

**단계 이동 규칙 (오버엔지니어링 방어 — ROADMAP_v1)**

1. 앞 단계 성공 기준을 통과하기 전에 다음 단계 코드를 작성하지 않는다.
2. 기능 추가 요청은 Lean Canvas ③ UVP의 두 축 — **"시간 축"과 "사진"** — 으로 필터링한다. 두 축 밖이면 기각하거나 v3+로 미룬다. 수익 모델(⑥)을 근거로 한 기능 추가는 제지한다.
3. **"나중에 쓸 거니까 미리"는 금지.** 데이터 모델·컴포넌트·유틸 모두 현 단계 범위만큼만 만든다.
4. 기술 선택마다 "왜 + 배제한 대안"을 기록한다.

## 개발 워크플로우

1. **작업 계획** — 코드베이스 현황 파악, ROADMAP.md 갱신.
2. **작업 생성** — Task별 명세 작성(관련 파일·수락 기준·구현 단계·테스트 체크리스트).
3. **작업 구현** — 명세 준수, 단계별 진행 상황 갱신, **각 Task 완료 후 중단하고 지시 대기**.
4. **로드맵 갱신** — 완료 Task ✅ 표시, 다음 "우선순위" Task 지정 (항상 정확히 1개).

## 기술 스택 (확정 — PRD 5장, 재논의 금지)

| 영역            | 선택                                     | 왜                                                                                        | 배제한 대안                                                                                   |
| --------------- | ---------------------------------------- | ----------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| 프레임워크      | Next.js 16 (App Router) + TypeScript     | 익숙한 스택으로 진짜 어려운 문제(그리드 인터랙션)에 집중                                  | —                                                                                             |
| 백엔드/DB       | Supabase (Auth + Postgres + RLS)         | Auth·DB·RLS 일괄 제공, 기존 경험 재활용                                                   | 자체 백엔드 (불필요한 초기 비용)                                                              |
| 인증            | 이메일+비밀번호                          | 1인 도구 — 이메일 의존 제거로 개발·테스트·실사용 마찰 제로 (2026-07-28 매직링크에서 전환) | 매직링크(내장 메일 2통/h 제한, 템플릿 편집에 SMTP 요구) / 소셜 로그인 (초기 과잉, v1+ 재검토) |
| 드래그/리사이즈 | 라이브러리 없이 pointer 이벤트 직접 구현 | "y축 픽셀 → 분 스냅"은 좁은 특수 문제, 범용 라이브러리는 과함                             | dnd-kit 등 (이 케이스에 과함)                                                                 |
| 상태관리        | React 내장 (useState/useReducer)         | v0 규모에 전역 상태 라이브러리 불필요                                                     | Redux/Zustand 등 (현 규모에 과함)                                                             |
| 배포            | Vercel                                   | Next와 자연스러운 통합                                                                    | —                                                                                             |
| 스타일          | Tailwind v4 + Broadsheet 토큰(CSS 변수)  | create-next-app에 이미 세팅됨, 핸드오프 토큰을 CSS 변수로 이식                            | CSS-in-JS (추가 의존성 불필요)                                                                |

**데이터 모델 (확정 — 두 테이블만)**: `tasks`(무엇을·좌측) 1 : N `blocks`(언제·우측). 시간은 timestamp가 아닌 **int(자정 기준 분)** — 그리드 렌더·겹침 검사·리사이즈가 단순 산술이 된다 (배제한 대안: timestamptz 2개 — 타임존·파싱 복잡도로 배제). status는 Postgres enum이 아닌 **text + 앱 레이어 TS union 타입**(`as`/`any` 금지) — 초기 스키마 경직성 회피 (배제한 대안: enum — `ALTER TYPE` 족쇄로 배제). 사진·서클 등 미래 테이블을 미리 만들지 않는다.

**블록 상태값 (확정)**: `planned` / `done` / `partial` / `moved`. **`missed`는 존재하지 않는다** — 실패를 전시하지 않는다(리텐션 방어, PRD 4.1).

---

## v0 개발 단계

**검증할 가설**: 타임박싱 절차(brain dump → TOP3 → 배치 → 기록) 자체가 매일 쓸 만한가?
**현재 코드베이스**: v0 기능 전체 완성 — brain dump → TOP3 → 배치/배치 취소 → 드래그/리사이즈 → 상태 토글 → 날짜 이동(URL `?date=` 컨텍스트, 마스트헤드 ‹어제 내일›·아침/저녁 토글, 전날 미배치 task 이월 `carryTaskToDate`). 남은 것은 Vercel 배포·실사용 검증(Task 009)뿐.

### Phase 1: 기반 (Task 001) ✅

#### Task 001: 프로젝트 셋업 및 Supabase 연동 ✅ - 완료

**목표**: Next.js 앱에 Supabase(Auth + Postgres + RLS)를 연결하고 매직링크 로그인이 실제 동작하는 상태를 만든다. (F8, F9 기반 / PRD 6장 Task 1)

> 2026-07-28 전환: 매직링크는 내장 메일 발송 제한(2통/h)·템플릿 편집 SMTP 요구로 **이메일+비밀번호 로그인으로 교체**됨 (기술 스택 표 참조). 아래 기록은 완료 당시 이력.

**착수 전**: `node_modules/next/dist/docs/01-app/` 가이드 선독 — 특히 라우팅·서버/클라이언트 컴포넌트·env 관련 항목. Next 16 breaking changes로 학습 데이터와 다를 수 있다.

**구현 범위**

- Supabase 프로젝트 생성, 환경 변수 연결 (`@supabase/supabase-js` + Next 16 호환 클라이언트 구성)
- `tasks`·`blocks` 두 테이블 스키마 생성 (PRD 4장 정의 그대로 — 컬럼 추가·미래 테이블 생성 금지)
- 두 테이블 모두 RLS 정책: `user_id = auth.uid()` 행만 select/insert/update/delete
- 이메일 매직링크 로그인/로그아웃 플로우 (로그인 페이지 + 콜백 처리)
- `BlockStatus` TS union 타입 정의 (`'planned' | 'done' | 'partial' | 'moved'`) — `as`/`any` 금지

**수락 기준**

- [x] 매직링크 이메일 수신 → 클릭 → 로그인 완료 → 앱 진입이 실제로 동작한다
- [x] `tasks`·`blocks` 테이블이 PRD 4장 스키마와 정확히 일치한다 (시간은 int 분, status는 text)
- [x] **RLS 검증: 타 유저(별도 테스트 계정)의 데이터가 조회·수정되지 않는다** — 실제 쿼리로 확인
- [x] 비로그인 상태에서 앱 데이터 접근이 차단된다

---

### Phase 2: 좌측 — 계획 도구 (Task 002~003) ✅

#### Task 002: Brain dump 패널 구현 ✅ - 완료

**목표**: 좌측 패널에서 할 일을 개수 제한 없이 추가·삭제하고, DB에 영속화한다. (F1)

**착수 전**: `node_modules/next/dist/docs/01-app/` 데이터 페칭·서버 액션 관련 가이드 선독.

**구현 범위**

- 2열 레이아웃 뼈대 (`grid-template-columns: 352px 1fr; gap: 40px`) + Broadsheet 토큰 이식(색·Source Serif 4 타이포·간격, 산세리프 도입 금지) — 참조: `docs/archive/design_handoff_day3box_v0/captures/01-morning-plan.png`
- `쏟아내기 · Brain dump` 섹션: 항목 행(체크박스형 사각 + 제목), 하단 점선 "＋ 할 일 추가…" 입력(엔터/버튼)
- 오늘 날짜 기준 task 추가/삭제 → Supabase `tasks` insert/delete, 로드 시 해당 날짜 task 조회
- 낙관적 업데이트 후 DB 반영 (상태는 useState/useReducer만 사용)

**수락 기준**

- [x] 항목 추가/삭제가 즉시 화면에 반영되고 DB에 저장된다
- [x] **엣지 케이스: 새로고침·재로그인 후에도 입력한 task가 유지된다** (미배치 task 영속 — 길 B)
- [x] 개수 제한 없이 입력 가능하다

**테스트 체크리스트 (Playwright MCP E2E)**

- [x] 로그인 → brain dump 항목 3개 입력 → 새로고침 → 3개 그대로 유지
- [x] 항목 1개 삭제 → 새로고침 → 삭제 상태 유지
- [x] 빈 입력 제출 시 항목이 생성되지 않는다

#### Task 003: TOP 3 선정 및 소요시간 입력 구현 ✅ - 완료

**목표**: brain dump 항목을 TOP 3로 승격/해제하고 소요시간(`est_min`)을 입력한다. **하드 제한 + 교체 UX**가 완료 정의에 포함된다. (F2 / PRD 7.1)

**착수 전**: `node_modules/next/dist/docs/01-app/` 가이드 선독 (Task 002와 동일 맥락).

**구현 범위**

- 승격/해제 (`is_top3` 토글), TOP3 카드에 `est_min`(분) 입력
- `TOP 3` 섹션 UI: 카드 3장(좌측 4px 카테고리색 바 + kicker + 제목 + 소요시간 태그), 빈 자리 점선 안내 "3개 채움 · 4번째는 하나를 내려야 올라온다" — 참조: `captures/01-morning-plan.png`
- 카테고리 **5색 프리셋** (`--cat1`~`--cat5`, 핸드오프 토큰 값): 색은 프리셋 고정, **이름만 편집 가능** (자유 색 입력 금지)
- **교체 모달**: 4번째 승격 시도 시 단순 차단이 아니라, 기존 TOP3 중 하나를 라디오로 선택해 내리고 새 항목을 올리는 모달 (막는 것이 아니라 선택을 강제한다 — PRD 7.1)

**수락 기준**

- [x] 승격/해제·`est_min`·카테고리가 DB에 저장되고 새로고침 후 유지된다
- [x] TOP3는 **하드 제한 3개** — 어떤 경로로도 4개가 될 수 없다
- [x] **엣지 케이스: 4번째 승격 시 교체 모달이 뜬다 (단순 차단 금지)** — 교체 선택 시 기존 항목 해제 + 새 항목 승격이 원자적으로 반영
- [x] 카테고리는 5색 프리셋 밖의 색을 지정할 수 없다

**테스트 체크리스트 (Playwright MCP E2E)**

- [x] 항목 3개 승격 → TOP3 카드 3장 표시 → 새로고침 유지
- [x] 4번째 승격 시도 → 교체 모달 표시 → 하나 선택해 교체 → 결과가 정확히 3개, 새로고침 후 유지
- [x] 교체 모달에서 취소 → TOP3 변동 없음
- [x] `est_min` 90 입력 → 새로고침 → 값 유지

---

### Phase 3: 우측 — 타임 그리드 (Task 004~006) ✅

#### Task 004: 정적 타임 그리드 구현 ✅ - 완료

**목표**: 우측에 06:00–24:00 세로 시간축 그리드를 안정적으로 렌더한다. (F3)

**착수 전**: `node_modules/next/dist/docs/01-app/` 클라이언트 컴포넌트·hydration 관련 가이드 선독 (현재 시각 스크롤은 클라이언트 전용 로직).

**구현 범위**

- **06:00–24:00 고정** 세로축·눈금선 (1시간 = 42px, 좌측 시각 라벨 거터 ≈ 56px — 핸드오프 눈금 명세)
- 현재 시각 마젠타 라인("지금 HH:MM") 표시
- **진입 시 현재 시각으로 자동 스크롤** (PRD 7장 #1 — 범위보다 스크롤 위치가 체감 UX를 좌우)
- 그리드 헤더(`타임박스` h6 + 범례) — 참조: `captures/01-morning-plan.png`
- 그리드는 독립 렌더 단위로 구성하되(구조 감각만), v1 이미지 캡처용 코드를 미리 작성하지 않는다

**수락 기준**

- [x] 06:00–24:00 눈금과 시각 라벨이 목업 눈금(42px/시간)대로 렌더된다
- [x] 페이지 진입 시 현재 시각 위치로 자동 스크롤된다
- [x] **엣지 케이스: 빈 하루(데이터 0)에서도 그리드 뼈대만으로 깨지지 않고 렌더된다**

**테스트 체크리스트 (Playwright MCP E2E)**

- [x] 로그인 후 데이터 0 상태 진입 → 06:00~24:00 시각 라벨·눈금선이 오류 없이 렌더
- [x] 진입 직후 스크롤 위치가 현재 시각 라인 근처에 있다
- [x] 현재 시각 라인의 top 좌표가 (현재시각-06:00) 분 × 0.7px(42px/시간)과 일치

#### Task 005: 블록 배치 구현 ✅ - 완료

**목표**: 좌측 task 클릭 → 우측 그리드의 다음 빈 슬롯에 블록으로 자동 배치하고 DB에 저장한다. (F4 / PRD 7장 #4)

**착수 전**: `node_modules/next/dist/docs/01-app/` 가이드 선독.

**구현 범위**

- 좌측 task(주로 TOP3) **클릭 → 다음 빈 슬롯 자동 배치** (클릭 지점 배치·drag-across는 v0 제외 — 실수 유발·과잉 구현으로 배제, PRD 5장·7장)
- 소요시간(`est_min`) = 블록 높이 (`height = est_min/60*42px`), 배치 시각은 15분 단위 정렬
- `blocks` insert: `task_id`·`date`(task.date 복사, 비정규화)·`start_min`·`end_min`·`status='planned'`
- 블록 렌더: 좌측 4px 카테고리색 바 + 제목(task에서 읽음 — 제목·카테고리는 task에만 두는 정규화)
- 배치 시 겹침 검사: 빈 슬롯 탐색이 기존 블록을 건너뛴다 (한 시간대 = 최대 한 블록)

**수락 기준**

- [x] task 클릭 → 현재 시각 이후 다음 빈 슬롯에 블록이 생성되고 DB에 저장, 새로고침 후 유지
- [x] 블록 높이가 `est_min`과 일치한다
- [x] **엣지 케이스: `est_min` 없는 TOP3는 배치가 차단되고 소요시간 입력을 요구한다**
- [x] **엣지 케이스: 기존 블록과 겹치는 슬롯에는 배치되지 않는다** (다음 빈 슬롯으로 밀림)

**테스트 체크리스트 (Playwright MCP E2E)**

- [x] 데이터 0인 날짜 진입 → 그리드 뼈대 정상 렌더 (Task 004 엣지 케이스)
- [x] `est_min` 90분 TOP3 클릭 → 블록 생성(높이 = 90분 상당) → 새로고침 → 블록 유지
- [x] `est_min` 미입력 TOP3 클릭 → 배치되지 않고 입력 요구 UI 노출
- [x] 블록 2개 연속 배치 → 서로 겹치지 않는 슬롯에 배치됨

#### Task 006: 블록 이동·리사이즈 구현 ✅ - 완료

**목표**: 배치된 블록을 pointer 드래그로 이동, 가장자리로 리사이즈한다. 15분 스냅 + 겹침 금지. (F5 / PRD 7장 #2)

**착수 전**: `node_modules/next/dist/docs/01-app/` 클라이언트 컴포넌트·이벤트 관련 가이드 선독.

**구현 범위**

- **라이브러리 없이 pointer 이벤트 직접 구현** (pointerdown/move/up + cleanup — "y축 픽셀 → 분" 변환은 좁은 특수 문제, dnd-kit 등 범용 라이브러리는 과함이라 배제, PRD 5장)
- 드래그 이동: y좌표 → 분 변환, **15분 스냅** (42px/시간 기준 15분 = 10.5px)
- 가장자리 리사이즈: 상/하단 핸들, 15분 스냅, `end_min > start_min` 보장
- **겹침 금지**: 이동·리사이즈 결과가 기존 블록과 겹치면 거부(스냅으로 밀거나 원위치 복귀)
- 드래그 중 임시 위치는 로컬 상태, 확정 시 `start_min`/`end_min` DB update

**수락 기준**

- [x] 드래그 이동·리사이즈가 15분 단위로만 스냅된다
- [x] **엣지 케이스: 겹침이 발생하는 이동·리사이즈는 거부된다**
- [x] 그리드 범위(06:00–24:00) 밖으로 이동·리사이즈되지 않는다
- [x] 변경이 DB에 저장되고 새로고침 후 위치·크기가 유지된다

**테스트 체크리스트 (Playwright MCP E2E)**

- [x] 블록 드래그 → 15분 경계 위치로 스냅 확인 → 새로고침 → 위치 유지
- [x] 하단 리사이즈로 30분 연장 → 높이·`end_min` 반영 → 새로고침 유지
- [x] 다른 블록 위로 드래그 → 겹침 거부(원위치 또는 인접 스냅) 확인

---

### Phase 4: 기록과 순환 (Task 007~008.7) ✅

#### Task 007: 블록 상태 토글 구현 ✅ - 완료

**목표**: 블록 상태를 `planned`/`done`/`partial`/`moved`로 전환하고, 선택 블록과 좌측 편집 카드를 연동한다. (F6)

**착수 전**: `node_modules/next/dist/docs/01-app/` 가이드 선독.

**구현 범위**

- 그리드 블록 클릭 → 선택(accent 2px 링) → 좌측 **상태 기록 편집 카드** 활성화 — 참조: `captures/02-evening-record.png`
- 세그먼트 컨트롤 [완료 | 부분 | 이월]로 상태 전환 → 블록 태그·색 즉시 갱신 + DB update
- 상태별 스타일 (핸드오프 상태 색): `planned` 표면색+카테고리 바 / `done` cyan 틴트 태그 / `partial` magenta 틴트 태그 / `moved` **점선 테두리 + 중립 회색 + "→ 내일로 이월"** — 빨강·실패 색 절대 금지
- `오늘의 기록` 요약(완료 n / 부분 n / 이월 n)과 `TOP 3 결과` 섹션
- 사진 추가·한 줄 회고·이미지로 저장은 목업에 있어도 **넣지 않는다** (v1)

**수락 기준**

- [x] 블록 선택 ↔ 좌측 편집 카드가 양방향으로 연동된다 (선택 시 카드 갱신, 상태 변경 시 블록 즉시 갱신)
- [x] 4개 상태 전환이 모두 동작하고 DB에 반영, 새로고침 후 유지된다
- [x] `moved` 블록이 점선+중립 회색으로 표시된다 (실패 전시 없음 — `missed` 문자열이 코드·UI 어디에도 없다)
- [x] 상태값이 TS union 타입으로만 다뤄진다 (`as`/`any` 없음)

**테스트 체크리스트 (Playwright MCP E2E)**

- [x] 블록 클릭 → accent 링 + 편집 카드 표시
- [x] 완료/부분/이월 각각 전환 → 태그·색 즉시 갱신 → 새로고침 → 상태 유지
- [x] 오늘의 기록 요약 숫자가 실제 블록 상태 집계와 일치

#### Task 008: 날짜 이동 구현 ✅ - 완료

**목표**: 어제/오늘/내일 이동 시 해당 날짜의 task·block이 함께 올바르게 로드된다. (F7)

**착수 전**: `node_modules/next/dist/docs/01-app/` 라우팅·검색 파라미터 가이드 선독.

**구현 범위**

- 마스트헤드 ‹어제 · 내일› 내비 + 날짜 표시, [아침 계획]/[저녁 기록] 토글 — 참조: `captures/01`, `02`
- 날짜 변경 시 해당 날짜의 `tasks`·`blocks` 함께 로드 (block 조회는 비정규화된 `block.date`로 조인 없이)
- **미배치 task 이월 노출**: 어제 쏟아냈지만 배치하지 않은 task가 오늘도 보이거나 오늘로 가져올 수 있다 ("오늘 못 한 걸 내일로" — PRD 3장, 길 B의 존재 이유). 이월 행에서 가져오기 또는 즉시 삭제 가능 (2026-07-29 확장 — 가져온 뒤 지우는 2단계 우회 제거)

**수락 기준**

- [x] 어제/오늘/내일 이동 시 그 날짜의 task·block만 정확히 표시된다 (날짜 간 데이터 섞임 없음)
- [x] **엣지 케이스: 어제의 미배치 task가 소실되지 않고 오늘 접근 가능하다**
- [x] 과거 날짜의 그리드(계획 대비 실제)가 그대로 보존되어 돌아볼 수 있다
- [x] 데이터 없는 날짜로 이동해도 빈 하루가 안정적으로 렌더된다

**테스트 체크리스트 (Playwright MCP E2E)**

- [x] 오늘 task·블록 생성 → 어제로 이동 → 오늘 데이터가 보이지 않음 → 오늘 복귀 → 데이터 정합
- [x] 어제 날짜에 미배치 task 생성 → 오늘에서 해당 task 접근·가져오기 동작
- [x] 내일로 이동 → 빈 그리드 정상 렌더 → 새로고침 → 날짜 컨텍스트 유지

#### Task 008.5: 블록 배치 취소 구현 ✅ - 완료

**목표**: 배치된 블록을 그리드에서 제거한다. task는 brain dump에 유지된다 (F4의 역연산 — PRD 명세 공백 보완, 2026-07-29 등재. 배치 실수 복구 경로가 없으면 실사용 이탈 요인).

**구현 범위**

- `deleteBlock` 서버 액션: 블록만 삭제, task 유지 (기존 액션 패턴 그대로)
- 저녁 기록 편집 카드에 "배치 취소" 버튼 (블록 클릭 시 기록 모드 자동 전환이라 아침 모드에서도 접근 가능)
- 그리드 블록 hover/선택 시 우측 상단 × 버튼 — 선택 없이 즉시 삭제하는 직접 진입점 (2026-07-29 사용자 요청으로 확장. 15분 블록은 공간 부족으로 생략, 편집 카드 경로 사용)
- 취소된 task는 재배치 가능하고, block 0개가 되므로 익일 이월 섹션 대상이 된다

**수락 기준**

- [x] 배치 취소 시 블록만 삭제되고 task는 brain dump 목록에 유지된다
- [x] 삭제가 DB에 반영되고 새로고침 후에도 유지된다
- [x] 취소 직후 동일 task를 다시 배치할 수 있다
- [x] 편집 카드가 플레이스홀더로 자연 복귀한다 (선택 해제)

**테스트 체크리스트 (Playwright MCP E2E)**

- [x] task 배치 → 블록 클릭 → 배치 취소 → 블록 소멸 + task 잔존 → 새로고침 → 상태 유지
- [x] 취소 직후 같은 task 재배치 → 정상 배치
- [x] 블록 hover → × 노출 → 클릭 → 선택/모드 전환 없이 즉시 삭제, 드래그 이동은 계속 정상 동작

#### Task 008.6: 배포 전 UX 보완 ✅ - 완료

**목표**: 실사용 테스트(2026-07-29)에서 확인된 4건 보완 — 일반 task 배치, 팔레트 유색화, 무채색 구분, 소요시간·블록 양방향 동기화. (PRD F4는 원래 "task(**주로** TOP3) 배치" — 일반 배치는 명세 방향과 일치)

**구현 범위**

- **일반 task 배치**: brain dump 행 hover '배치' 버튼 → 소요시간 미지정이면 **기본 30분** 즉시 배치(세밀 조정은 그리드 리사이즈). TOP3의 소요시간 입력 요구는 유지 (배제한 대안: 행별 분 입력 UI — 마찰·복잡도 증가로 배제)
- **팔레트 유색화**: cat4 회색 → 보라 `#7b52c7`, cat5 남색 → 초록 `#2f8f4e` (남색은 파랑과 유사, 회색은 유색 구분 원칙 위반 — 2026-07-29 오너 결정으로 핸드오프 토큰 값 변경). moved(이월) 스타일은 신규 중립 토큰 `--color-neutral`(구 회색값)로 분리해 회색 유지
- **무채색 구분**: 일반(무카테고리) task 블록은 중립 회색 바 — TOP3(유채색)와 시각 구분. TOP3는 승격 시 카테고리 미지정이면 **미사용 프리셋 색 자동 지정**(무조건 유색)
- **양방향 동기화**: 카드 분 수정 → 배치 블록 높이 재계산(겹침·범위 초과 시 전체 거부), 블록 리사이즈 → 카드 분 갱신. 기본 30분 배치 시 est_min도 30으로 저장 (배제한 대안: 비연동 유지 — est_min·블록 불일치 혼란으로 배제)

**수락 기준**

- [x] est_min 없는 일반 항목 배치 → 30분 블록 생성 + est_min=30 저장, 무채색 회색 바
- [x] est_min 없는 TOP3 배치는 여전히 차단·입력 요구 (PRD 엣지 케이스 유지)
- [x] 항목 3개 연속 승격 → 서로 다른 유채색 자동 지정, 새로고침 유지
- [x] 스와치 5색 모두 유채색, moved 블록·범례·요약은 중립 회색 유지
- [x] 분 수정 ↔ 블록 리사이즈 양방향 반영, 겹침 발생 시 분 수정 거부·블록 불변

**테스트 체크리스트 (Playwright MCP E2E)**

- [x] 일반 항목 hover → 배치 → 30분 블록 + 회색 바 확인
- [x] 3개 승격 → 3색 상이 확인 → 새로고침 유지
- [x] 분 30→90 수정 → 블록 높이 반영 / 리사이즈 → 분 갱신 / 겹침 유발 수정 → 에러
- [x] moved 상태 전환 → 회색 점선 유지

#### Task 008.7: 그리드 가독성 개선 ✅ - 완료

**목표**: 짧은(15~30분) 블록에서 제목·시간이 보이게 하고, "지금 HH:MM" 라벨이 블록 텍스트에 가려지지 않게 한다. (실사용 테스트 2026-07-29 지적 — 배포 전 처리: 그리드 가독성은 연속 3일 실사용 검증의 전제)

**구현 범위**

- **눈금 명세 변경**: 1시간 42px(구 목업) → **60px (1분=1px)** — 2026-07-29 신규 시안 `docs/archive/design_handoff_day3box_v0/captures/03-edit-timebox.png` 채택. 15분 스냅 = 15px 정수 (배제한 대안: 42px 유지+폰트 축소 — 15분 블록 10.5px는 어떤 폰트로도 수납 불가). 시안의 06–22시 범위는 채택하지 않고 v0 확정 06–24시 유지
- **길이별 3단계 블록 레이아웃**: ≥45분 2줄(기존) / 30~44분 한 줄(제목 + 시간 우측) / <30분 초경량 한 줄(제목 + `HH:MM · N분`)
- **지금 라벨 우측 이동**: 블록 텍스트가 좌측 정렬이라 우측은 늘 여백 → 겹침 해소

**수락 기준**

- [x] 15분·30분 블록에서 제목과 시간이 읽힌다
- [x] 블록 top = (start_min − 360) × 1px, 드래그·리사이즈 15분 스냅(15px) 정상
- [x] 지금 라벨이 우측에 표시되어 블록과 겹쳐도 가려지지 않는다
- [x] 리사이즈로 길이 경계(45/30분) 통과 시 레이아웃이 자연 전환된다

**테스트 체크리스트 (Playwright MCP E2E)**

- [x] 15분·30분·90분 블록 각각 배치 → 세 티어 렌더·텍스트 가시성 확인
- [x] 드래그 이동 → 15px 단위 스냅 → 새로고침 위치 유지
- [x] 오늘 그리드에서 지금 라벨이 우측 렌더 확인

---

### Phase 5: 배포 및 실사용 검증 (Task 009) ✅

#### Task 009: Vercel 배포 및 실사용 시작 ✅ - 완료

**목표**: 프로덕션 배포 후 본인 실사용을 시작하고, v0 성공 기준(연속 3일)을 추적한다. (PRD 1.6)

**착수 전**: `node_modules/next/dist/docs/01-app/` 배포·env 관련 가이드 선독.

**구현 범위**

- Vercel 프로덕션 배포 (환경 변수, Supabase Auth 리다이렉트 URL 프로덕션 도메인 등록)
- 배포 환경에서 전체 플로우 스모크 테스트: 로그인 → brain dump → TOP3·교체 모달 → 배치 → 이동/리사이즈 → 상태 토글 → 날짜 이동
- 본인 실사용 시작: 아침 계획 + 저녁 기록

> **2026-07-30 배포 완료**: 프로덕션 URL `https://day3box-app.vercel.app` (Vercel 대시보드 GitHub 연동 — main push마다 자동 배포). env는 `NEXT_PUBLIC_SUPABASE_URL`·`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` 2개만. 인증이 이메일+비밀번호라 리다이렉트 URL 등록은 불필요했음(Site URL 설정만 선택 사항).
>
> **2026-07-30 실사용 1일차 피드백 → 성능 개선**: 날짜 이동·TOP3 변경→그리드 동기화가 1초+ 지연. 측정 결과 원인은 **Vercel 함수 리전(기본 iad1·미국 동부)과 Supabase(서울) 불일치** — 렌더당 순차 왕복 6회(proxy getUser + page getUser + 쿼리 4) × 서울↔미국 200ms. 해결: `vercel.json regions:["icn1"]`(함수 서울 고정) + page 쿼리 4개 `Promise.all` 병렬화 + `loading.tsx`(전환 즉시 피드백). 배제한 대안: proxy getClaims 로컬 검증·그리드 낙관 동기화 확장 — 리전 정렬 후 재측정으로 필요 시에만.
>
> **개선 수치**: 개선 후 실측 — 로그인 세션 포함 날짜 뷰 서버 렌더 **363~416ms**(브라우저 fetch 3회). 개선 전은 구조 기반 추정 — 순차 6왕복 × 서울↔iad1 RTT ~190ms ≈ **1.3~1.6초**(서버 렌더만), 체감 2~3초(콜드 스타트·클라이언트 렌더 포함 — 실사용 관찰과 일치). 약 **3~4배 단축**. 정밀 A/B(iad1 프리뷰 배포 재현)는 Vercel 배포 보호(SSO)로 외부 측정이 막혀 생략 — 필요 시 Deployment Protection 해제 후 baseline 브랜치 재생성으로 가능.

**수락 기준**

- [x] 프로덕션 URL에서 F1~F9 전체 플로우가 동작한다 (2026-07-30 Playwright 스모크: 로그인 → brain dump 4건 → TOP3 3개 승격·자동 색 → 4번째 교체 모달 → est_min 입력·배치 + 일반 task 30분 배치 → 드래그 15분 스냅 → 완료 토글·요약 집계 → 새로고침 영속 → 익일 이동·빈 그리드·이월 노출 확인 후 테스트 데이터 정리)
- [x] 본인이 실제 하루 계획·기록에 사용하기 시작했다 (2026-07-31 실사용 1일차 개시)

**실사용 추적 (v0 통과 판정)**

- [x] 1일차 (2026-07-31): 아침 계획 + 저녁 기록 — 피드백으로 Brief 004(행 재설계) 반영
- [x] 2일차 (2026-08-01): 아침 계획 + 저녁 기록 — 피드백으로 Brief 005(드래그 마감·제목 편집) 반영
- [x] 3일차 (2026-08-02): 아침 계획 + 저녁 기록 → **연속 3일 달성 — v0 통과** (Brief 006 §배경)
- ~~통과하지 못하면 v1으로 넘어가지 않고 v0를 고친다~~ → 통과. 마지막 개선 사이클(Phase 8) 후 v1 계획 수립 개시

#### Task 009.5: 낙관 반영 전면 확장 ✅ - 완료

**목표**: 색·분·배치·삭제·이월이 서버 응답을 기다리지 않고 화면에 즉시 반영된다. (실사용 1일차 피드백 — 리전 정렬 후에도 그리드 갱신은 서버 왕복 후였음)

**구현 범위**

- 낙관 상태를 DayView 단일 스토어(`{tasks, blocks, carryTasks}`)로 통합 — 블록의 제목·색·높이를 낙관 tasks에서 클라이언트 파생 (서버 조인 제거)
- 색 변경 → 블록 바 즉시 / 분 변경 → 블록 높이 즉시(클라이언트 겹침·범위 선검사, 충돌 시 즉시 에러) / task 삭제 → 블록 즉시 소멸(cascade 미러) / 배치 취소·이월 가져오기·삭제 즉시 반영
- 배치 낙관: 슬롯 탐색을 `lib/grid.ts findNextFreeSlot`으로 추출해 서버·클라이언트 공용 (중복 없음, 정상 흐름에서 낙관 슬롯 = 서버 슬롯). temp 블록은 서버 확정 전 조작 차단
- PlanPanel은 프레젠테이션으로 축소 (교체 모달 로컬 상태와 하드 제한 선분기만 유지) — 서버는 계속 단일 진실(검증·거부), 클라이언트는 미리보기

**수락 기준**

- [x] 색·분 변경이 카드와 그리드 블록에 대기 없이 동시 반영된다 (50ms 내 assert)
- [x] 배치 클릭 시 블록이 즉시 등장하고, 서버 확정 후에도 같은 슬롯이다
- [x] 분 변경이 겹침·범위 초과를 유발하면 즉시 에러가 뜨고 화면·서버 모두 불변이다
- [x] task 삭제·배치 취소·이월 조작이 즉시 반영되고 새로고침 후에도 정합하다
- [x] 기존 회귀 없음: 드래그 스냅·상태 토글·교체 모달·자동 색·날짜 이동

**테스트 체크리스트 (Playwright MCP E2E)**

- [x] 색/분 변경 → 대기 없이(50ms) 블록 색·높이 assert → 새로고침 정합
- [x] 배치 → 즉시 등장 → 새로고침 후 동일 슬롯
- [x] 겹침 유발 분 변경 → 즉시 에러 + 불변
- [x] task 삭제 → 블록 동시 소멸 (이월은 동일 run() 경로 — 서버 흐름 기존 검증 유지)

---

### Phase 6: 실사용 피드백 반영 (Task 010~013) ✅

> **근거 문서**: `docs/briefs/DECISION-BRIEF-002.md`(D1~D6) · `docs/briefs/DECISION-BRIEF-003.md`(D7·D8 + 미결 4건) · **`docs/archive/handoff-v0-002/`(v0 UI 단일 진실 — 구 핸드오프 폐기)**. 소스 대조 결과 D7(강등 시 블록 유지)·D8(배율 상수 파생)은 이미 충족. 2026-07-31 오너 확정 3건: est↔블록 **비동기화**(양방향 롤백, ADR-0002) / 그리드 **04:00~익일 03:00**(D9 — 핸드오프 06–22·PRD 06–24 대체) / fixed 블록 **상태·집계 제외**. 핸드오프의 "한 줄 회고"는 PRD §2.2 제외 규율대로 구현하지 않는다.

#### Task 010: 시간 체계 정비 (미결1·D5·D6·D9) ✅ - 완료

**목표**: 스냅·소요시간·그리드 범위·est↔블록 관계를 하나의 일관된 10분 체계로 정비한다. 나머지 Task의 전제.

**구현 범위**

- 스냅 15분 → **10분** (`SNAP_MIN` — 끝점이 격자를 벗어나는 근본 원인 D5 해소)
- 그리드 **04:00 ~ 익일 03:00** (23h·1380px). 하루 경계 04시 — 새벽 3시까지는 전날의 하루 (`todayInSeoul`·`nowMinutesInSeoul`), 자정 넘김 라벨 00:00~03:00
- 소요시간 입력: 자유 입력 제거 → **칩(30/60/90/120/180) + ±10 스테퍼** (가짜 정밀 제거 — D6). est는 10~1440·10분 배수(서버+DB CHECK)
- **est↔블록 비동기화**: 리사이즈는 est를 갱신하지 않고, est 수정은 블록을 건드리지 않는다. est=계획 총량, 블록 합=배치량, 차이=미배치 잔량 (양방향 동기화 롤백 — ADR-0002)
- DB 마이그레이션: est_min·blocks start/end 10분 라운딩(겹침 사전 확인) + est CHECK 제약

**수락 기준**

- [x] 드래그·리사이즈·배치가 10분 격자에만 스냅되고, 블록 끝점이 항상 격자 위에 온다
- [x] 그리드가 04:00~익일 03:00을 렌더하고, 새벽(04시 이전) 접속 시 "오늘"이 전날로 유지된다
- [x] 소요시간은 칩·스테퍼로만 입력되고 10분 배수 외 값이 DB에 들어갈 수 없다 (CHECK 제약)
- [x] 리사이즈 후 카드 분이 변하지 않고, 분 수정 후 블록이 변하지 않는다 (비동기화 — ADR-0002)
- [x] 기존 데이터가 10분 격자로 정리되고 겹침이 없다 (est 3건·blocks 12건 라운딩, 겹침 0)

**테스트 체크리스트 (Playwright MCP E2E)**

- [x] 블록 드래그 → 10분 경계 스냅(08:30 = 270px) → 새로고침 유지
- [x] 칩 90 선택 → 카드 90분·chip-on / +10 두 번 → 110분·칩 미선택 / 자유 입력 UI 부재
- [x] 배치 후 리사이즈(110→180분) → 카드 분 110 불변 / 칩 60 변경 → 블록 180분 불변
- [x] 눈금 04:00~03:00 라벨(자정 넘김 00~03시)·그리드 1380px 정상

#### Task 011: 블록 분할 (D4) ✅ - 완료

**목표**: 긴 TOP3 작업이 점심 등으로 끊길 때 같은 task를 여러 블록으로 배치한다 (1 task : N blocks — 스키마 기존 지원, UI만 해제).

**구현 범위**

- **배치 클릭 = 미배치 잔량(est − 배치 합)을 다음 빈 슬롯에** (역제안 — 리사이즈로 줄인 뒤 재클릭하면 잔량이 배치됨. 신규 UI 없음)
- 분할 블록 n/N 표기 + 잘린 변 스타일 (핸드오프 §5 — `↳` 금지, 색 막대 비관통)
- TOP3 카드에 `총 N분 · M분 배치됨`/`미배치` 표시 (메타 문구 상수 분리)
- 기록의 TOP3 결과 대표 상태: 다중 블록 규칙 정의

**수락 기준**

- [x] 180분 task를 30분으로 리사이즈 후 재배치 클릭 → 잔량 150분이 다음 빈 슬롯에 배치된다
- [x] 분할 블록들이 같은 색·제목 + 1/2·2/2 표기 + 잘린 변(첫 조각 하단 라운드 0, 뒤 조각 상단 파선·14px 들여쓰기)으로 렌더된다
- [x] 카드가 배치량을 정확히 표시한다 (`총 180분 중 30분 배치됨` → 전량 시 `180분 배치됨`, 잔량 0 재클릭 시 안내 에러)
- [x] PRD §2.2에서 분할 UI가 v0로 이동되고 이유가 기록된다 (기록 대표 상태: 일부만 완료 → `부분` 검증 포함)

#### Task 012: 고정 시간 (D2·D1·D3/1b) ✅ - 완료

**목표**: 식사·이동·휴식 등 "확보한 시간"을 그리드 드래그로 직접 만들고, 할 일과 시각적·의미적으로 구분한다.

**구현 범위**

- `tasks.kind` (`'task'`/`'fixed'`) 마이그레이션 — 두 값 구분이지 카테고리 시스템이 아님 (경계선 준수)
- 그리드 빈 영역 드래그 생성 (고스트 4a: 하단 정보·10분 눈금 드래그 중만·겹침 표시 크림슨 금지) → 이름 입력 → `kind='fixed'` task+block 생성
- fixed는 brain dump 미표시·TOP3 승격 불가·이월 제외·**상태 토글과 기록 집계 제외**(오너 확정)·마지막 블록 삭제 시 task 동반 삭제(고아 방지 — 브리프 보완)
- 1b 시각: 막대 없음 + 본문 옅게 + 아주 옅은 외곽선(빈 시간과 정지 이미지에서도 구분), 범례 3위계

**수락 기준**

- [x] 빈 그리드 드래그 → 고스트(하단 정보·10분 눈금) → 이름 입력 → 고정 블록 생성, 새로고침 유지
- [x] 고정 항목이 좌측 어디에도 나타나지 않고 이월되지 않는다 (이월은 kind='task' 필터 + 배치된 fixed는 구조적으로 미배치 불가)
- [x] 고정 블록이 기록 요약(완료/부분/이월)에 집계되지 않는다 (편집 카드도 상태 세그먼트 없이 "확보한 시간" 안내)
- [x] 고정 블록 전부 삭제 시 유령 task 행이 남지 않는다 (SQL 확인: ghost_fixed_tasks 0)
- [x] 컬러 막대(TOP3)/회색 막대(일반)/막대 없음(고정) 3위계가 렌더되고 범례가 교체됐다 (겹침 드래그는 중립 실선 고스트로 거부, 무이동 클릭은 생성 안 됨)

#### Task 013: TOP3 교체 제자리 UX + 상태 표식 (D7/5a·6b) ✅ - 완료

**목표**: 교체를 모달 없이 좌측 제자리에서 처리하고, 블록 상태를 우측 인쇄 표식으로 통일한다.

**구현 범위**

- 교체 5a: 카드 클릭=선택 → 하단 "바꾸기"=확정. 카드에 `N분 · HH:MM 배치됨`/`미배치` 표시. 문구 "일반 할 일이 됩니다. 배치는 그대로 유지됩니다" (취소선·"안 됨" 어휘 금지). `<dialog>` 제거 — v0에 모달 0개
- 상태 표식 6b: □(계획)·■(완료)·◧(부분)·▨(이월) 우측 11px 표식, 본문 무게 4상태 동일. 이월에 날짜 표기 금지, 부분에 비율 표기 금지 (미결 3·4 — 저장 안 함 확정)
- 핸드오프 §9 검수 체크리스트 전체 수행

**수락 기준**

- [x] 4번째 승격 → 좌측 제자리 선택 모드(배치 정보 표시) → 교체 후 정확히 3개, 강등된 task의 블록이 그대로(막대만 회색 — 블록 색 파생을 is_top3 조건으로 수정해 관통 규칙 1 봉인)
- [x] 4상태가 표식(□■◧▨)으로 구분되고 본문 무게가 동일하다 (이월에 빨강·취소선·날짜 없음, 점선 스타일 제거)
- [x] 앱에 모달 표면이 0개다 (dialog 제거, HOUR_PX 90 전환 파생 테스트 통과 — 핸드오프 §9 검수)

---

### Phase 7: 실사용 피드백 2차 (Task 014~015) ✅

> **개선 사이클 마감 (Brief 005 §9)**: D11~D15로 이 사이클을 닫는다. 새 개선 항목을 받기 전에 **3일 기록 상태를 확인하고 v1 착수 여부를 판단한다** — 계속 다듬으면 "완성도"라는 이름으로 v1(사진)이 밀린다.

> **근거 문서**: `docs/briefs/DECISION-BRIEF-004.md`(D9) · `docs/handoff-v0-003/`(CHANGE-002 — v0 UI 단일 진실, v0-002 대체). D10(완료 표시)은 이번 사이클 제외 — 미결 5번(다중 블록 완료 정의: 모두 done=완료/일부=부분, Task 011 기록 대표 상태와 동일 규칙)만 기록. 오너 확정 2건(2026-08-01): 드롭 길이 = est ?? 30분(위치 지정만, 길이는 리사이즈) / 재드래그 = 항상 잔량 배치, 전량 배치 행은 드래그 불가(이동은 그리드에서 — CHANGE-002 §6 "재드래그=이동"은 다중 조각 모호성으로 채택 안 함).

#### Task 014: Brain dump 행 재설계 + 리스트 드래그 배치 (D9) ✅ - 완료

**목표**: 체크박스(완료 통념·저빈도 동작이 진입점) 혼동 제거 — 행을 [⠿ 손잡이|색 막대|이름|힌트|TOP3 배지]로 재설계하고, 손잡이 드래그로 원하는 시각에 직접 배치한다.

**구현 범위**
- 체크박스·[TOP 3] 배지·"배치 →" 버튼 제거 → `.grip`(클릭=다음 빈 슬롯, 드래그=시각 지정) + `.bar3`(TOP3만 컬러) + `.hint` + `.tbtn` 토글 (CHANGE-002 §2)
- 리스트→그리드 드래그: `placeBlock(taskId, startMin?)` 확장(신규 액션 없음), DayView가 window pointer 추적·가장자리 자동 스크롤(48px·∓12)·고스트(기존 `.ghost-block` 재사용)
- 배치 표시는 밝기 한 채널(opacity 0.72 — 시안 0.62는 비활성 우려로 상향)·자리 이동 없음(입력 순 보존, D9-5)·헤더 "N개 남음"(D9-6)

**수락 기준**
- [x] 좌측 목록에 체크박스 형태 요소가 0개다
- [x] 손잡이 클릭 → 다음 빈 슬롯 배치 / 드래그 → 드롭 지점이 시작 시각(10분 스냅), 겹침 위치는 고스트 invalid·드롭 거부
- [x] TOP3 배지 토글이 승격/강등·교체 트리거를 그대로 수행하고 배치 후에도 유지된다 (D9-2)
- [x] 배치 행이 0.72로 물러나되 자리는 그대로, 잔량 남은 행 재드래그 = 잔량 배치(분할), 전량 배치 행은 드래그 미시작 + 안내
- [x] "N개 남음"이 미배치 수와 일치하고 새로고침 후 정합하다

#### Task 015: 드래그 마감·클릭 배치 규칙·제목 인라인 편집 (Brief 005 D11~D15) ✅ - 완료

**목표**: Task 014 실사용에서 발견된 5건 — 드래그 프리뷰 미완성 마감(D11), 배치 구분 강화(D12), 예측 가능한 클릭 배치(D13), 행 전체 드래그+제목 인라인 편집(D14), TOP3 카드 드래그(D15). 미결 1번(est↔블록 비동기)은 **확정으로 승격** (ADR-0002 + Task 014 구현이 전제 — 재론 금지).

**구현 범위**
- D11: **커서 추종 프리뷰**(`이름 · N분`, fixed·ref 직접 갱신 — portal 불필요 확인) — 리스트·그리드 어디서든 항상, 그리드에선 스냅 고스트와 동시 표시
- D12: 배치 행 opacity 0.72 → **0.5** (보조 채널은 실사용 후 판단)
- D13: 클릭 배치 = **마지막 블록 뒤**(`findAfterLastSlot` — max end 뒤라 겹침 검사 불요), 안 되면 빈 슬롯 폴백 + 배치 위치로 스크롤 알림
- D14: **행/카드 전체 드래그**(임계 5px 공유 상수) + 손잡이 클릭=배치·제목 클릭=편집·배지=토글 역할 분리. **제목 인라인 편집**(`renameTask`·`InlineTitle` 공용 — Enter/blur 저장·Esc 취소·빈 값 원복·편집 중 드래그 차단). 블록 제목도 동일 규칙 + **표식 클릭 = 상태 순환**. 팝오버 없음
- D15: TOP3 카드 드래그 배치(리스트 로직 그대로 재사용) + 카드 손잡이 + 제목 편집 (칩·스테퍼 무간섭 — 5px 임계)
- E2E 중 발견·수정: 잔량 검사를 pointerdown → 드래그 확정 시점으로 이동(전량 배치 카드의 칩 클릭마다 에러가 뜨던 문제), 한 줄 티어에서 hover ×가 표식을 덮던 겹침(pr 확보)

**수락 기준**
- [x] 드래그 시작 직후부터 프리뷰가 커서를 따라오고, 그리드 진입 시 프리뷰+고스트 동시 표시
- [x] 클릭 배치가 마지막 블록 직후에 붙는다 (폴백 시 스크롤 알림)
- [x] 행 여백 드래그로 배치되고, 제목 클릭 편집이 리스트·카드·블록 모두에서 동작하며 블록 제목이 낙관 파생으로 즉시 갱신된다
- [x] Esc·빈 값은 원복(삭제 아님), 드래그 후 유령 클릭 없음, 칩·스테퍼·배지 클릭 무간섭
- [x] 표식 클릭으로 4상태가 순환한다 (fixed 제외)

---

### Phase 8: 실사용 피드백 3차 — v0 마감 (Task 016) ✅

> **v0 개선 종료 게이트 (Brief 006 §6)**: v0 성공 기준 통과(7/31·8/1·8/2). D16~D19 완료 후 **새 개선 항목을 받지 않고 v1으로 넘어간다**. 이후 발견되는 불편함은 적어만 두고 v1 이후에 다룬다 (예외: 사용을 완전히 막는 버그뿐).

> **근거 문서**: `docs/briefs/DECISION-BRIEF-006.md`. 소스 대조 결과 — D16은 낙관 누락이 아니라 form action이 서버 왕복을 await해 React 폼 리셋이 지연되는 구조(낙관 추가 자체는 Task 009.5에서 이미 구현됨).

#### Task 016: v0 마지막 개선 — 입력 지연·부분 배치·탭 분리·초과 표시 (Brief 006 D16~D19) ✅ - 완료

> **D16 구현 노트 (2026-08-03)**: 원인은 브리프 추정(낙관 누락)과 달리 form action이 서버 왕복을 await해 React 19의 자동 폼 리셋이 지연되는 구조였다. run() 편입만으로는 부족 — 동기 action도 내부 startTransition과 entangle되어 리셋이 왕복 뒤로 밀리는 것을 실측(~566ms)으로 확인, `onSubmit` + 수동 초기화로 전환(제출 직후 0~1ms). 지연 자동 리셋이 타이핑 중 텍스트를 지우는 문제도 함께 제거.

**목표**: 실사용 3일에서 나온 마지막 4건 — task 추가 입력 지연 해소(D16), 빈 구간 부분 배치(D17), 블록 선택·탭 전환 분리(D18), 계획 총량 초과 병기 표시(D19).

**구현 범위**
- D16: brain dump form action을 동기화 — `handleAdd`를 `run()` 낙관 패턴에 편입, 입력창 리셋·focus가 서버 왕복과 무관하게 즉시
- D17: `lib/grid.ts` `freeSpanAt`(가용 구간 길이) 신설 → 드래그 고스트가 빈 구간 크기로 잘리고(`60분 · 남은 80분` 라벨) 잘린 만큼만 배치. `placeBlock(taskId, startMin?, durationMin?)` 확장 — 서버는 durationMin을 신뢰하지 않고 DB 잔량 상한·겹침·그리드 끝 재검증(조용한 보정 없음). 최소 크기 제한 없음, 그리드 끝 동일 규칙. **클릭 배치는 제외**(고스트가 없어 잘림 예고 불가 — D13 예측 가능성 유지)
- D18: `handleSelect`의 `setMode('record')` 제거 — 탭 이동은 탭 버튼만. 선택 ↔ 편집 카드 연동(Task 007)·표식 상태 순환(D14)은 유지
- D19: `placementLabel` 초과 분기 분리 — `210분 / 계획 180분` 병기, 옅은 강조만(경고색·아이콘 금지). 선택사항("계획을 210분으로")은 제외

**수락 기준**
- [x] 항목 5개를 쉬지 않고 연속 타이핑 입력해도 매 제출 직후 입력창이 비어 있고 포커스가 유지된다 (D16)
- [x] 잔량 140분 task를 60분 빈 구간에 드롭 → 드래그 중 고스트가 60분으로 잘려 보이고(`60분 · 남은 80분`) 드롭 시 60분 블록 + 잔량 80분 (D17)
- [x] 그리드 끝(익일 03:00)에서도 남은 공간만큼 잘려 배치되고, 블록 위 드롭은 여전히 거부된다 (D17)
- [x] 블록 클릭 시 탭이 전환되지 않고 선택 링만 표시, 기록 탭 진입 시 선택 블록이 편집 카드에 연동된다 (D18)
- [x] 배치 합계가 계획 총량을 넘으면 `N분 / 계획 M분` 병기(경고색 없음), 줄이면 `N분 배치됨` 복귀 (D19)

**테스트 체크리스트 (Playwright MCP E2E)**
- [x] D16: 5회 연속 requestSubmit 직후 동기 캡처 — `input.value === ''` && focus 유지, 목록 5개, 새로고침 정합
- [x] D17: 60분 gap 드롭(고스트·라벨·블록·카드 문구), 그리드 끝 잘림, invalid 드롭 무시, 새로고침 후 크기 유지
- [x] D18: 계획 탭 블록 클릭 → `aria-pressed` 유지 + 선택 링, 기록 탭 직접 진입 → 편집 카드 연동
- [x] D19: est 감소로 초과 유발 → 병기 문구·에러색 아님, 복귀 확인

---

## v1 개발 단계

### Phase 9: v1-R3 — 사진 입력 마찰 검증 (Task 017~019)

> **검증할 가설 (R3, Lean Canvas — v1 최우선)**: *사람들이 매일 사진을 찍어 넣는 마찰을 감수하는가?* 사진은 텍스트 체크보다 훨씬 무거운 행동이며, 이것이 안 되면 v1 전체가 무너진다.
>
> **검증 순서 (Brief 006 §6 확정)**: 사진을 *어떻게 보여줄지*(시안 11안·내보내기 프레임·콜라주)는 결정하지 않는다. **"매일 찍어 넣는가"만** 최소 비용으로 먼저 확인한다 — 표현을 먼저 정하면 검증되지 않은 가설 위에 UI를 쌓게 된다.
>
> **R3 통과선 (2026-08-04 오너 확정 — v0 "연속 3일"에 대응하는 선행 지표)**:
> - **통과**: 검증 개시일부터 7일간, 하루 1장 이상 사진을 넣은 날이 **6일 이상** (결측 1일 허용 — "찍을 블록이 없는 날"과 마찰 실패를 구분하는 완충)
> - **조기 실패**: 연속 2일 0장이면 7일을 기다리지 않고 즉시 실패 판정
> - **실패 시**: 마찰 원인을 당일 기록 — ① 찍는 것 자체가 귀찮음 / ② 브라우저 진입이 귀찮음 / ③ 업로드 UX 문제. ②·③이면 경로 A 안에서 1회 개선 후 7일 재시도. ①이거나 재시도 실패면 **"앱으로만 해결되는 마찰인지" 논증한 뒤에만** 경로 B(앱) 검토 — 앱도 ①은 해결하지 못하며, 이 구분이 B 전환 근거의 전부다
> - 검증 중 ②③형 마찰 개선을 반영해도 **카운트는 리셋하지 않는다** — 목적은 습관 형성 관찰이 아니라 마찰 존재 확인
>
> **오너 확정 4건 (2026-08-04)**: 통과선 7일 중 6일 / 모바일 범위는 **기록 중심**(아침 계획은 데스크톱 유지) / 실기기 iPhone iOS 17+ (WebP 저장 가능) / **한 블록 = 사진 1장** (1:N은 표현 단계에서 필요 시)
>
> **v1 공식 통과 조건**(본인 외 사용자의 이미지 다운로드·공유)은 R3 다음 단계(표현·내보내기)의 것이다. R3는 v0와 같은 본인 선행 검증.

#### Task 017: 기반 정리 — 마이그레이션 드리프트 백필 + 모바일 1열 - 우선순위

**목표**: 사진 마이그레이션에 앞서 로컬·원격 마이그레이션 이력을 정합시키고, 모바일(390px)에서 기록 동선(열람·탭 전환·블록 선택·편집 카드·brain dump 입력)이 동작하는 1열 레이아웃을 만든다.

**구현 범위**
- **드리프트 백필**: 원격에만 적용된 2건(`20260731093844 ten_minute_grid`, `20260731121139 tasks_kind`)을 원격 스키마에서 역산해 `supabase/migrations/` SQL 파일로 백필. 기존 로컬 2건과의 파일명·version 정합도 함께 정리 (원격 스키마 변경 0 — 파일 작업만)
- **모바일 1열**: `day-view.tsx` `grid-cols-[352px_1fr]` → 모바일 1열·데스크톱 2열 분기(프로젝트 최초의 반응형 분기점), 우측 컬럼 구분선 조정 / `time-grid.tsx` `max-h-[calc(100vh-240px)]` 모바일 대응(`dvh` 검토) / globals.css 블록 좌표는 확인 후 필요한 것만
- `DRAG_THRESHOLD_PX` 5 → 8 (lib/grid.ts 예약 주석 — 터치 탭이 드래그로 오인되는 것 방지)
- **하지 않는 것**: hover 전용 UI(행 삭제 ×·top3 도구·힌트) 터치화, 모바일 드래그·리사이즈 — 아침 계획은 데스크톱에서 한다 (오너 확정)

**수락 기준**
- [ ] `supabase migration list` 기준 로컬·원격 4건 일치, 원격 스키마 변화 0
- [ ] 390px 뷰포트: 계획·기록 탭 열람, 블록 탭 선택 → 편집 카드 연동, brain dump 입력·Enter 추가 동작, 가로 스크롤 없음
- [ ] 데스크톱 2열·드래그·리사이즈·부분 배치 회귀 없음

**테스트 체크리스트 (Playwright MCP E2E)**
- [ ] `browser_resize` 390×844 → 1열 렌더·탭 전환·블록 탭 선택·편집 카드·task 추가 확인
- [ ] 1280px → 2열 복귀, 리스트 드래그 배치·블록 이동 회귀 확인
- [ ] 실기기(iPhone)에서 열람·탭·선택 확인 (배포 후)

#### Task 018: 사진 입력 수직 슬라이스 (블록에 사진 1장)

**목표**: 기록 탭 편집 카드에서 사진을 찍어 넣으면(촬영→압축→업로드) 블록에 존재 표식이 뜨고 새로고침 후에도 유지된다. UI+DB+Storage가 함께 완성되는 수직 슬라이스.

**구현 범위**
- **마이그레이션 1건**: `photos` 버킷(**private**) + storage RLS(`(storage.foldername(name))[1] = auth.uid()::text`, select·insert·update·delete) + `blocks.photo_path text null`·`blocks.photo_added_at timestamptz null` (기존 테이블 RLS가 그대로 커버) → 타입 재생성
- **경로 규칙**: `{user_id}/{date}/{block_id}.webp` — 교체는 upsert 덮어쓰기 (고아 파일 최소화, 청소 배치 없음)
- **압축 (`lib/image.ts` 신규, 라이브러리 금지)**: `createImageBitmap(file, {imageOrientation:'from-image'})`(EXIF 회전) → canvas → `toBlob` — **긴 변 1600px, WebP 0.8** (미지원 브라우저 JPEG 0.82 fallback). 원본 저장 금지(정본 확정)
- **진입점**: 기록 탭 편집 카드에 "사진 추가" — `<input type="file" accept="image/*" capture="environment">` + 업로드 스피너(카드 로컬 상태) + 소형 썸네일 + 교체·삭제. 그리드 블록에는 **존재 표식(작은 도트)만** — 그리드 썸네일은 표현 11안 선점이므로 금지
- **업로드 경로**: 브라우저 → Storage **직접 업로드**(upsert) → 성공 후 서버 액션 `setBlockPhoto(blockId, path)`가 경로 프리픽스(`{user.id}/`) 검증 후 기록. 삭제는 `removeBlockPhoto` — storage remove 시도 후 성공 여부와 무관하게 컬럼 null 우선(보상 패턴). 조회는 `createSignedUrl`(1시간)
- **낙관 반영**: reducer에 `setPhoto`/`removePhoto` 액션, 기존 `run()` 패턴 (업로드 진행 중은 카드 로컬 스피너 — 파일이 없으면 낙관할 대상이 없음)
- **ADR-0003(사진 저장·압축 설계) 확정 승격** — 초안(제안)을 구현 결과로 확정

**수락 기준**
- [ ] iPhone 실기기: 촬영 → 압축 → 업로드 → 표식 표시까지 한 손 동선으로 완료, 새로고침 정합
- [ ] 저장 파일이 긴 변 ≤1600px·≤500KB, 세로 사진 회전 정상
- [ ] 타 계정 경로 업로드·조회가 storage RLS로 거부된다 (SQL/API 확인)
- [ ] 같은 블록에 재업로드 시 Storage 파일이 1개로 유지된다 (덮어쓰기)
- [ ] 데스크톱에서도 파일 선택으로 동일 동작, 기존 기능 회귀 없음

**테스트 체크리스트 (Playwright MCP E2E + 실기기)**
- [ ] 미래 날짜에서: 블록 생성 → 사진 첨부(`browser_file_upload`) → 표식·썸네일 확인 → 새로고침 정합 → 교체 → 삭제 → 표식 소멸 (끝나면 데이터·Storage 정리)
- [ ] 압축 검증: 4000px급 이미지 업로드 → Storage 객체 크기·해상도 확인
- [ ] 실기기(iPhone): 카메라 capture 촬영 업로드 1회 이상

#### Task 019: R3 실사용 검증 사이클 (코드 없음 — 게이트)

**목표**: 통과선 기준으로 7일 실사용 판정을 내리고 다음 행동을 확정한다. v0 "성공 기준 통과 전 다음 단계 금지" 규율의 재사용 — **이 게이트를 통과하기 전에 표현 단계(시안 선택·이미지 다운로드) 코드를 작성하지 않는다.**

**구현 범위**
- 검증 개시일 기록 → 7일 실사용 (아침 계획 데스크톱 + 기록·사진은 모바일)
- 마찰 로그 수기 기록 (①②③ 분류, 날짜별)
- 보조 지표는 Supabase SQL 수기 집계 (통과선 아님): 완료 블록 대비 사진 비율 / `photo_added_at`으로 당일 즉시 vs 저녁 몰아넣기 판별
- 판정 및 다음 행동(표현 단계 착수 / A 개선 1회 후 재시도 / B 전환 논증)을 이 절에 기록

**수락 기준**
- [ ] 통과선(7일 중 6일 / 조기 실패) 기준의 판정이 날짜·근거와 함께 기록됨
- [ ] 판정에 따른 다음 행동이 ROADMAP에 명시됨

---

## v1 이후 (Phase 9 이후 범위 — R3 게이트 통과 전 착수 금지)

아래는 범위 요약만 둔다. **R3 게이트(Task 019) 통과 전에는 표현 단계 코드를 작성하지 않는다.**

- **v1 표현·공유 단계 (R3 통과 후)**: 사진 표현 방식 선택(시안 11안 — `docs/handoff-v0-003/reference/day3box-photo-v1-exploration.dc.html`), `HOUR_PX 60→90` 전환, 하루/주간 플래너 이미지 다운로드(렌더링 방식: 클라이언트 캡처 vs 서버 렌더링 이때 결정), `moved` 블록의 이미지 내 표현("실패를 전시하지 않는다"). v1 통과 조건: **본인 외 사용자의 이미지 다운로드·공유**.
- **v2 — 프라이빗 서클**: 초대 기반 그룹(3~5명), 서로의 플래너 보기. 공개 소셜 메커니즘(팔로워·좋아요·랭킹) 금지. 북극성 지표: 그룹당 주간 활성 멤버 수 (DAU 아님).
- **v3+ (논의 보류)**: 달성률 차트, 반복 블록, 알림, task 다중 블록 분할 UI, 소셜 로그인, 실시간 동기화, 태그·검색·필터, 다크모드/테마.

> 목업에 그려져 있어도 v0에서 만들지 않는 것: 사진 슬롯, 이미지로 저장 버튼, 주간/서클 내비, 한 줄 회고 (디자인 핸드오프 스코프 표).

---

TODO IDEA

- 랜덤 명언, 동기부여 문구 삽입
  - 동기부여 문구 카테고리화 -> 운동선수, 게이머, 강사, 기업인, 종교적 등
- 사진 공유 필터 다양화
  - 해외 다크 모티베이션 컨셉, 공주, 고3, 취준생, 쉬었음청년 필터 등
